# Maxfield's Pancake House — QA Round 2

- Date: 2026-05-06
- Gate: `qa_round_2`
- Stage advanced to: `qa_round_2`
- Archetype: `Cuisine`
- Template: `plate-01`
- Preview URL: `https://skills-git-feat-maxfields-p-46b25d-ethan-ethantalrejas-projects.vercel.app`

## QA checks performed

- Re-read the required round context: `checklist.json`, QA round 1 notes, pitch doc, battle cards, `content.ts`, and `app/layout.tsx`.
- Re-checked the explicit QA1 regressions in code:
  - `app/layout.tsx` still exports the mobile viewport.
  - contact-page dedupe is still present.
  - shared hours rows still stack on mobile through `components/HoursTable.tsx`.
  - QA1 screenshot evidence paths remain present in checklist metadata.
- Captured and reviewed fresh QA2 desktop/mobile screenshots for homepage and contact page via parent host Chrome/CDP against the local Next build.
- Re-checked truth safety across homepage, contact, and concierge content for hours uncertainty, ordering uncertainty, reservation handling, quotes, awards, and prices.
- Re-ran required verification commands.

## Screenshot / evidence status

- Fresh QA2 visual evidence captured from the local Next build with parent host Chrome/CDP:
  - `restaurant-website-system/sites/maxfields-pancake-house/screenshots/qa-round-2-home-desktop.png`
  - `restaurant-website-system/sites/maxfields-pancake-house/screenshots/qa-round-2-home-mobile.png`
  - `restaurant-website-system/sites/maxfields-pancake-house/screenshots/qa-round-2-contact-desktop.png`
  - `restaurant-website-system/sites/maxfields-pancake-house/screenshots/qa-round-2-contact-mobile.png`
- Capture dimensions:
  - Desktop: 1440 × 1400
  - Mobile: 390 × 1100
- DOM overflow check during capture: `scrollWidth === viewport width` on homepage and contact mobile/desktop.

## Findings

### Finding 1 — Mobile viewport and CTA path still hold

- The fresh QA2 mobile screenshots show the device-width viewport fix holding.
- No horizontal overflow or clipped CTA rows were detected in the CDP overflow check or final image review.
- The required owner demo path remains obvious:
  - `Call (847) 781-0300`
  - `Get directions`
  - `See menu highlights`

### Finding 2 — Desktop hero proof block needed a stronger first-view contrast treatment

- The first viewport remained structurally correct, but the right-side recovery card and the visit-confidence hours card were reading a little pale relative to the hero headline.
- That was not a truth or layout bug, but it did soften the sell-readiness of the first desktop impression.
- Fix applied:
  - increased background contrast and edge definition in `components/HeroSplit.tsx`
  - increased contrast and emphasis in `components/VisitConfidencePanel.tsx`

### Finding 3 — Truth safety still holds

- No fake exact hours were introduced.
- No fake reservation flow, fake direct ordering provider, fake quotes, fake awards, or fake prices were introduced.
- Hours uncertainty is still disclosed clearly and routed back to `(847) 781-0300`.
- Ordering remains cautious and does not claim a verified owner-controlled provider.

### Finding 4 — QA1 regression fixes remain intact

- `app/layout.tsx` still exports the required viewport object.
- The contact page still avoids the duplicate closing CTA block that QA1 removed.
- `HoursTable` still stacks safely on smaller screens.
- QA1 screenshot evidence remains wired into `checklist.json` and `checklist.md`.

## Fixes made

- `components/HeroSplit.tsx`
  - strengthened the recovery card background and edge contrast
  - made the recovery sub-cards more defined
  - raised the note contrast so the owner-facing recovery story reads more clearly on desktop
- `components/VisitConfidencePanel.tsx`
  - strengthened the hours/source-comparison card contrast
  - promoted the eyebrow styling so the section reads less washed out

## Verification

- `npm run typecheck` — failed first because this project includes `.next/types/**/*.ts` and the generated files were missing before a fresh build
- `npm run build` — passed
- `npm run typecheck` — passed after build regenerated `.next/types`
- Forbidden placeholder grep — passed with no output

## Remaining blockers

- Mission Control writeback remains locally prepared only because auth is unavailable in this runtime.
- Public hours still conflict across sources, so final publish should still rely on owner or Google Business Profile confirmation.
- A direct owner-controlled online ordering provider is still unverified.

## Result

QA round 2 passes locally for Maxfield's with one focused desktop sell-readiness polish fix, fresh desktop/mobile screenshot evidence, verification passing, truth safety preserved, and QA1 regressions still intact.
